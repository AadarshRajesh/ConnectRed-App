import 'package:flutter/material.dart';

class Screen5Directions extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Screen 5: Directions')),
      body: Center(child: Text('Directions Page Content')),
    );
  }
}
